name: EKS Cluster Management Build Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: kube-kaptain/buildon-github-actions/.github/workflows/aws-eks-cluster-management.yaml@1.1.19
    permissions:
      contents: write
      packages: write
      checks: write
